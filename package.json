{
  "name": "GitDojo",
  "version": "1.0.0",
  "description": "GitDojo is a web app that allows you to track your progress on github and compare it to other users",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd client && npm run dev\" \" cd server && npm run dev\" "
  },
  "repository": {
    "type": "git"
  },
  "author": "matthieu felker",
  "license": "ISC",

  "homepage": "",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
